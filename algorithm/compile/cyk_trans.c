#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#define MAX_SIZE 50
#define INF INT_MAX
typedef struct CFG {
	char V[MAX_SIZE];
	char T[MAX_SIZE];
	char S;
	char P[MAX_SIZE][3];
} CFG

int main(void) {
	CFG cfg;
	memcpy(cfg.V, "")
}
