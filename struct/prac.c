#include <stdio.h>
int main(void)
{

}
//6, 1, "Spiffo Road", 'f', 'f'
//
// struct month {
// int month;
// int days;
// char abbr[4];
// };
//
//typedef struct {}
int sumDays(char name[], int cols)
{
	int sum = 0;
	for (int i = 0; i < month; i++)
		sum += months[i].days

	return sum;
}
LENS arr[10]
typedef LENS arr[10];
arr lenarr;
lenarr[2].foclen = 500.0;
lenarr[10] = {
	[2]= {
	};
};
//6, "Arcturan",  "cturan"
//strct {
//} sr
void showDetail(const struct bem *ptr)
{
	printf("%s %s is a %d-limbed %s",
			ptr->title.first, ptr->title.last, ptr->limbs, ptr->type);
}
//willie.born, pt->born, 
//scanf("%d",&willie.born)
//scanf("%d",&pt->born)
//scanf("%s",willie.name.lname)
//scanf("%s",pt->name.lname)
//willie.name.fname[2], pt->name.fname[2]
//strlen(willie.name.fname)
// struct car_info {
// char name[40];
// int horsepower;
// }
//
//
struct gas mes(struct gas item)
{

}
enum choices {no, yes, maybe}
char *(*ptr)(char *, char);
double mp(double, double);
double (*ptr[4])(double, double);

