#include <stdio.h>
int main(int n, char *args[])
{
	for (int i = 0; i < n; i++)
		printf("%s \n", args[i]);
#include <string.h>
#include <ctype.h>
#include <stdlib.h>
int main(int n, char *args[])
{
	int tes = 455;
	char ie = tes;
	printf("%d",ie);
	// char **end;
	// printf("%lld", strtoll("9223372036854775807", end, 10));
   /*  char str[] = "Im Fine"; */
	// char *ptr = str;
	// if (!strcmp(args[1], "-p"))
		// printf("%s", str);
	// if (!strcmp(args[1], "-u"))
	// {
		// while (*ptr)
		// {
			// if (islower(*ptr))
				// *ptr = toupper(*ptr);
			// ptr++;
		// }
		// printf("%s", str);
	// }
	// if (!strcmp(args[1], "-l"))
	// {
		// while (*ptr)
		// {
			// if (isupper(*ptr))
				// *ptr = tolower(*ptr);
			// ptr++;
		// }
		// printf("%s", str);
	/* } */
	getchar();

	return 0;
}
