#include <stdio.h>
#include <stdlib.h>
#define SIZE 100
int main(void)
{
	int arr[SIZE];
	srand((unsigned)time(0));
	for (int i = 0; i < SIZE; i++)
		arr[i] = rand() % 10 + 1;
	for(int i = 0; i < SIZE - 1; i++)
		
}
